// Worker
window.addEventListener('load', ()=> {
  if (window.pdfjsLib) {
    window.pdfjsLib.GlobalWorkerOptions.workerSrc =
      'https://cdnjs.cloudflare.com/ajax/libs/pdf.js/3.11.174/pdf.worker.min.js';
  }
});
const SEARCH_API_PRIMARY='https://majestic-beijinho-cd3d75.netlify.app/.netlify/functions/search-media';
const SEARCH_API_SECONDARY='';
const XANO_PROXY_BASE='https://majestic-beijinho-cd3d75.netlify.app/.netlify/functions/xano-proxy';
const ASSIGNMENTS_REFRESH_MS=30000;
let unifiedResults=[],assignments=[],selectedUnified=null,playing=null,connGood=false,searchEndpointChosen=null,activeMediaEl=null;
const keyButtons=[...document.querySelectorAll('.key[data-key]')],stopButton=document.getElementById('stopButton'),assignmentsList=document.getElementById('assignmentsList'),assignmentsListManager=document.getElementById('assignmentsListManager'),connectionStatus=document.getElementById('connectionStatus'),searchInput=document.getElementById('searchInput'),mediaBrowser=document.getElementById('mediaBrowser'),selectedMedia=document.getElementById('selectedMedia'),keySelect=document.getElementById('keySelect'),titleInput=document.getElementById('titleInput'),descriptionInput=document.getElementById('descriptionInput'),stationInput=document.getElementById('stationInput'),tagsInput=document.getElementById('tagsInput'),submittedByInput=document.getElementById('submittedByInput'),assignButton=document.getElementById('assignButton'),alertBox=document.getElementById('alert'),currentInfo=document.getElementById('currentInfo'),currentTitle=document.getElementById('currentTitle'),currentMeta=document.getElementById('currentMeta'),mediaModal=document.getElementById('mediaModal'),modalClose=document.getElementById('modalClose'),modalTitle=document.getElementById('modalTitle'),mediaPlayer=document.getElementById('mediaPlayer'),mediaDescription=document.getElementById('mediaDescription'),sheet=document.getElementById('sheet'),sheetHeader=document.getElementById('sheetHeader'),sheetResize=document.getElementById('sheetResize'),tapOverlay=document.getElementById('tapOverlay'),tapPlayBtn=document.getElementById('tapPlayBtn'),titleOptions=document.getElementById('titleOptions'),stationOptions=document.getElementById('stationOptions');
const esc=s=>(s||'').toString().replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function show(t,m){alertBox.className='alert '+(t||'');alertBox.textContent=m;alertBox.style.display='block';clearTimeout(show._t);show._t=setTimeout(()=>{alertBox.style.display='none'},t==='error'?6000:3000)}
function setConn(ok){connGood=!!ok;connectionStatus.textContent=ok?'Connected':'Connection Error';connectionStatus.classList.toggle('bad',!ok)}
async function fetchJSON(u,o){const r=await fetch(u,o);if(!r.ok)throw new Error(r.status+' '+r.statusText);return r.json()}
async function xano(e,o={}){try{const r=await fetch(`${XANO_PROXY_BASE}/${e}`,{headers:{'Content-Type':'application/json'},...o});if(!r.ok)throw new Error(r.status+' '+r.statusText);const j=await r.json();setConn(true);return j}catch(err){console.error('Xano error:',err);setConn(false);show('error','Xano: '+err.message);return null}}
const LS_TITLES='voxpro_titles',LS_STATIONS='voxpro_stations';const readSet=k=>{try{return new Set(JSON.parse(localStorage.getItem(k)||'[]'))}catch{return new Set()}},writeSet=(k,s)=>localStorage.setItem(k,JSON.stringify([...s].slice(0,300)));
function addMemory(){const t=(titleInput.value||'').trim(),s=(stationInput.value||'').trim();if(t){const set=readSet(LS_TITLES);set.delete(t);set.add(t);writeSet(LS_TITLES,set)}if(s){const set=readSet(LS_STATIONS);set.delete(s);set.add(s);writeSet(LS_STATIONS,set)}populateDatalistsFromMemory()}
function populateDatalistsFromMemory(){const titles=[...readSet(LS_TITLES)],stations=[...readSet(LS_STATIONS)];titleOptions.innerHTML=titles.slice(-200).reverse().map(v=>`<option value="${esc(v)}">`).join('');stationOptions.innerHTML=stations.slice(-200).reverse().map(v=>`<option value="${esc(v)}">`).join('')}
function detectType(item){const t=(item.file_type||'').toLowerCase();if(t)return t;const u=(item.media_url||item.database_url||item.file_url||item.url||'').toLowerCase();if(/\.(mp3|m4a|aac|wav|ogg|flac)(\?|$)/.test(u))return'audio';if(/\.(mp4|mov|mkv|webm)(\?|$)/.test(u))return'video';if(/\.(png|jpg|jpeg|gif|webp|avif)(\?|$)/.test(u))return'image';if(/\.pdf(\?|$)/.test(u))return'pdf';if(/\.(doc|docx)(\?|$)/.test(u))return'doc';if(/\.(xls|xlsx|csv)(\?|$)/.test(u))return'sheet';return''}
async function createVideoThumb(url){return new Promise(res=>{try{const v=document.createElement('video');v.muted=true;v.preload='metadata';v.src=url+(url.includes('#')?'':'#t=0.2');v.playsInline=true;v.onloadeddata=()=>{const c=document.createElement('canvas');c.width=128;c.height=128;const g=c.getContext('2d');const r=Math.min(128/v.videoWidth,128/v.videoHeight),w=v.videoWidth*r,h=v.videoHeight*r;g.fillStyle='#0b0b0b';g.fillRect(0,0,128,128);g.drawImage(v,(128-w)/2,(128-h)/2,w,h);res(c.toDataURL('image/png'))};v.onerror=()=>res(null)}catch{res(null)}})}
let _pdfReady=null;function loadPdfJs(){if(_pdfReady)return _pdfReady;_pdfReady=new Promise((res,rej)=>{function go(){try{if(!window.pdfjsLib)throw new Error('pdf.js missing');window.pdfjsLib.GlobalWorkerOptions.workerSrc='https://cdnjs.cloudflare.com/ajax/libs/pdf.js/3.11.174/pdf.worker.min.js';res(window.pdfjsLib)}catch(e){rej(e)}}if(window.pdfjsLib)return go();const t=setInterval(()=>{if(window.pdfjsLib){clearInterval(t);go()}},200);setTimeout(()=>{if(!window.pdfjsLib){rej(new Error('pdf.js not loaded'))}},5000)}).catch(()=>null);return _pdfReady}
async function pdfPageThumb(url){const pdfjs=await loadPdfJs();if(!pdfjs)return null;try{const pdf=await pdfjs.getDocument(url).promise;const page=await pdf.getPage(1);const v=page.getViewport({scale:.5});const c=document.createElement('canvas');const g=c.getContext('2d');c.width=128;c.height=128;const s=Math.min(c.width/v.width,c.height/v.height);const v2=page.getViewport({scale:s});const off=document.createElement('canvas');off.width=v2.width;off.height=v2.height;await page.render({canvasContext:off.getContext('2d'),viewport:v2}).promise;g.fillStyle='#0b0b0b';g.fillRect(0,0,c.width,c.height);g.drawImage(off,(c.width-off.width)/2,(c.height-off.height)/2);return c.toDataURL('image/png')}catch{return null}}
const audioWaveThumbCache=new Map();async function renderAudioWaveThumb(url){if(audioWaveThumbCache.has(url))return audioWaveThumbCache.get(url);try{const AC=window.AudioContext||window.webkitAudioContext;if(!AC)return null;const ctx=new AC();const resp=await fetch(url,{mode:'cors'});if(!resp.ok)throw new Error('audio fetch failed');const buf=await resp.arrayBuffer();const audio=await ctx.decodeAudioData(buf);const ch=audio.getChannelData(0);const c=document.createElement('canvas');c.width=128;c.height=128;const g=c.getContext('2d');g.fillStyle='#0b0b0b';g.fillRect(0,0,128,128);g.strokeStyle='#00ff88';g.lineWidth=1;g.beginPath();const samples=1000,step=Math.max(1,Math.floor(ch.length/samples));for(let x=0;x<128;x++){const start=Math.min(ch.length-1,Math.floor((x/128)*samples)*step);const end=Math.min(ch.length-1,start+step);let min=1,max=-1;for(let i=start;i<end;i++){const v=ch[i];if(v<min)min=v;if(v>max)max=v}const y1=64+min*60,y2=64+max*60;g.moveTo(x,y1);g.lineTo(x,y2)}g.stroke();const data=c.toDataURL('image/png');audioWaveThumbCache.set(url,data);return data}catch{return null}}
async function createThumbnail(item){const mediaUrl=item.media_url||item.database_url||item.file_url||item.url||'';const turl=item.thumbnail||'';const type=detectType(item);if(turl){return `<img src="${esc(turl)}" onload="this.style.opacity=1" onerror="this.style.display='none'" style="opacity:0;transition:opacity .3s">`}if(type.includes('image')&&mediaUrl){return `<img src="${esc(mediaUrl)}" onload="this.style.opacity=1" onerror="this.style.display='none'" style="opacity:0;transition:opacity .3s">`}if(type.includes('video')&&mediaUrl){const data=await createVideoThumb(mediaUrl);return data?`<img src="${data}">`:`<video muted preload="metadata" playsinline><source src="${esc(mediaUrl)}#t=1"></video>`}if(type.includes('pdf')&&mediaUrl){const data=await pdfPageThumb(mediaUrl);return data?`<img src="${data}">`:'<div class="icon">📄</div>'}if(type.includes('audio')&&mediaUrl){const data=await renderAudioWaveThumb(mediaUrl);return data?`<img src="${data}" alt="waveform">`:'<div class="icon">🎵</div>'}if(type.includes('doc'))return'<div class="icon">📝</div>';if(type.includes('sheet'))return'<div class="icon">📊</div>';return'<div class="icon">📁</div>'}
async function unifiedSearch(term=''){if(unifiedSearch._t)clearTimeout(unifiedSearch._t);return new Promise(resolve=>{unifiedSearch._t=setTimeout(async()=>{try{const q=term.trim();const endpoints=searchEndpointChosen?[searchEndpointChosen]:[SEARCH_API_PRIMARY,SEARCH_API_SECONDARY].filter(Boolean);let data=null,lastErr=null;for(const ep of endpoints){try{const url=ep+'?'+(q?('q='+encodeURIComponent(q)+'&'):'')+'limit=100';data=await fetchJSON(url);searchEndpointChosen=ep;break}catch(e){lastErr=e}}if(!data){const assets=await xano('asset');if(!assets)throw lastErr||new Error('No search endpoint responded');const qlc=q.toLowerCase();const filtered=(assets||[]).filter(a=>{const hay=[a.title,a.description,a.station,a.tags,a.submitted_by].map(v=>(v||'').toLowerCase()).join(' ');return !qlc||hay.includes(qlc)}).map(x=>({id:'xano:'+x.id,source:'xano',title:x.title||'Untitled',description:x.description||'',station:x.station||'',tags:x.tags||'',thumbnail:x.thumbnail||'',media_url:x.database_url||x.file_url||x.url||'',file_type:x.file_type||'',submitted_by:x.submitted_by||'',created_at:x.created_at||''}));data={results:filtered}}unifiedResults=data.results||[];renderMediaBrowser();setConn(true);resolve(unifiedResults)}catch(e){console.error(e);setConn(false);show('error','Search failed');resolve([])}},180)})}
function renderMediaBrowser(){if(!unifiedResults.length){mediaBrowser.innerHTML='<div class="row"><div class="info">No media found</div></div>';return}mediaBrowser.innerHTML='';unifiedResults.forEach(async r=>{const row=document.createElement('div');row.className='row';row.dataset.id=String(r.id);const thumb=document.createElement('div');thumb.className='thumb';thumb.innerHTML='<div class="icon">📁</div>';try{thumb.innerHTML=await createThumbnail(r)}catch{}const info=document.createElement('div');info.className='info';info.innerHTML=`<div class="title">${esc(r.title||'Untitled')}</div><div class="meta">${esc(r.station||'Unknown')} • ${esc(r.source||'')}</div>`;row.appendChild(thumb);row.appendChild(info);row.addEventListener('click',()=>{mediaBrowser.querySelectorAll('.row').forEach(n=>n.classList.remove('selected'));row.classList.add('selected');const asset=unifiedResults.find(x=>String(x.id)===String(r.id));if(asset)selectUnifiedAsset(asset)});row.addEventListener('dblclick',()=>{const asset=unifiedResults.find(x=>String(x.id)===String(r.id));if(asset)openMediaModal(asset)});mediaBrowser.appendChild(row)})}
function selectUnifiedAsset(asset){selectedUnified=asset;selectedMedia.textContent=`Selected (${asset.source}): ${asset.title||'Untitled'}`;titleInput.value=asset.title||'';descriptionInput.value=asset.description||'';stationInput.value=asset.station||'';tagsInput.value=asset.tags||'';submittedByInput.value=asset.submitted_by||'';const editable=asset.source==='xano';[titleInput,descriptionInput,stationInput,tagsInput,submittedByInput].forEach(i=>{i.readOnly=!editable})}
async function loadAssignments(){const data=await xano('voxpro_assignments');if(!data){assignments=[];forceUpdateUI();return}assignments=data.sort((a,b)=>(a.key_number||0)-(b.key_number||0));for(const a of assignments){if(a.asset_id!=null){const asset=await xano('asset/'+a.asset_id);a.asset=asset||{id:a.asset_id,title:`Missing Asset ${a.asset_id}`,station:'Unknown',file_type:'unknown'}}else{a.asset={title:'No Asset ID',station:'Unknown',file_type:'none'}}}forceUpdateUI()}
function forceUpdateUI(){renderAssignments();updateKeyButtons();renderAssignmentsManager()}
function renderAssignments(){if(!assignments.length){assignmentsList.innerHTML='<div class="row"><div class="info">No assignments yet</div></div>';return}assignmentsList.innerHTML=assignments.map(a=>{const asset=a.asset||{};return `<div class="row" data-assign-id="${a.id}"><div class="thumb"><div class="icon">📁</div></div><div class="info"><div class="title">Key ${a.key_number||'?'} — ${esc(asset.title||'Unknown')}</div><div class="meta">${esc(asset.station||'Unknown')} • ${esc(asset.file_type||'')}</div></div></div>`}).join('');assignmentsList.querySelectorAll('.row').forEach(async (row,idx)=>{const a=assignments[idx];if(!a||!a.asset)return;const thumb=row.querySelector('.thumb');try{thumb.innerHTML=await createThumbnail(a.asset)}catch{}})}
function renderAssignmentsManager(){if(!assignments.length){assignmentsListManager.innerHTML='<div class="row"><div class="info">No assignments yet</div></div>';return}assignmentsListManager.innerHTML=assignments.map(a=>{const asset=a.asset||{};return `<div class="row"><div class="info"><div class="title">Key ${a.key_number||'?'}</div><div class="meta">${esc(asset.title||'Unknown')} • ${esc(asset.station||'Unknown')}</div></div><div><button class="btn btn-danger" data-del-id="${a.id}">Remove</button></div></div>`}).join('');assignmentsListManager.querySelectorAll('[data-del-id]').forEach(btn=>{btn.addEventListener('click',()=>deleteAssignment(btn.getAttribute('data-del-id')))})}
function updateKeyButtons(){keyButtons.forEach(btn=>{btn.classList.remove('assigned','playing');const key=Number(btn.dataset.key);const asn=assignments.find(a=>Number(a.key_number)===key);if(asn)btn.classList.add('assigned')});reflectPlaying()}
function reflectPlaying(){keyButtons.forEach(btn=>btn.classList.remove('playing'));if(playing&&playing.key){const btn=keyButtons.find(b=>Number(b.dataset.key)===Number(playing.key));if(btn)btn.classList.add('playing');currentInfo.style.display='block';currentTitle.textContent=playing.asset?.title?`Now Playing — ${playing.asset.title}`:'Now Playing';currentMeta.textContent=[playing.asset?.station||'',playing.asset?.file_type||''].filter(Boolean).join(' • ')}else{currentInfo.style.display='none'}}
async function ensureXanoAssetFromUnified(asset){if(!asset)return null;if(asset.source==='xano'){const idStr=String(asset.id||'').split(':')[1]||String(asset.id);const nid=Number(idStr);return Number.isFinite(nid)?nid:null}const payload={title:asset.title||'Untitled',description:asset.description||'',station:asset.station||'',tags:asset.tags||'',thumbnail:asset.thumbnail||'',database_url:asset.media_url||asset.url||'',file_url:asset.file_url||'',file_type:asset.file_type||'',submitted_by:asset.submitted_by||''};const created=await xano('asset',{method:'POST',body:JSON.stringify(payload)});return created&&created.id?Number(created.id):null}
async function assignSelectedToKey(){const key=Number(keySelect.value);if(!key){show('error','Choose a key slot');return}if(!selectedUnified){show('error','Select a media item first');return}const assetId=await ensureXanoAssetFromUnified(selectedUnified);if(!assetId){show('error','Could not resolve asset');return}if(selectedUnified.source==='xano'){const updates={title:titleInput.value||'',description:descriptionInput.value||'',station:stationInput.value||'',tags:tagsInput.value||'',submitted_by:submittedByInput.value||''};await xano('asset/'+assetId,{method:'PUT',body:JSON.stringify(updates)})}addMemory();const existing=assignments.find(a=>Number(a.key_number)===key);if(existing){await xano('voxpro_assignments/'+existing.id,{method:'PUT',body:JSON.stringify({key_number:key,asset_id:assetId})})}else{await xano('voxpro_assignments',{method:'POST',body:JSON.stringify({key_number:key,asset_id:assetId})})}show('success','Key assigned');await loadAssignments();updateKeyButtons()}
async function deleteAssignment(id){if(!id)return;await xano('voxpro_assignments/'+id,{method:'DELETE'});show('success','Assignment removed');await loadAssignments()}
function clearActiveMedia(){try{if(activeMediaEl){activeMediaEl.pause?.();activeMediaEl.src='';}}catch{}activeMediaEl=null}
async function ensurePlayable(el){try{await el.play();tapOverlay.style.display='none'}catch{tapOverlay.style.display='flex';tapPlayBtn.onclick=async()=>{try{await el.play();tapOverlay.style.display='none'}catch{}}}}
function openMediaModal(asset){mediaPlayer.innerHTML='';mediaPlayer.appendChild(tapOverlay);mediaDescription.textContent=asset.description||'';modalTitle.textContent=asset.title||'Player';const url=asset.media_url||asset.database_url||asset.file_url||asset.url||'';const type=(asset.file_type||detectType(asset)||'').toLowerCase();let el=null;if(type.includes('audio')){const wrap=document.createElement('div');wrap.style.display='grid';wrap.style.gridTemplateRows='1fr auto';wrap.style.height='100%';const canvas=document.createElement('canvas');canvas.width=460;canvas.height=220;canvas.style.margin='8px';canvas.style.background='#0b0b0b';const audio=document.createElement('audio');audio.controls=true;audio.autoplay=false;audio.style.width='100%';audio.playsInline=true;const src=document.createElement('source');src.src=url;audio.appendChild(src);wrap.appendChild(canvas);wrap.appendChild(audio);mediaPlayer.prepend(wrap);activeMediaEl=audio;try{const AC=window.AudioContext||window.webkitAudioContext;if(AC){const ctx=new AC();const analyser=ctx.createAnalyser();analyser.fftSize=2048;const msrc=ctx.createMediaElementSource(audio);msrc.connect(analyser);analyser.connect(ctx.destination);const g=canvas.getContext('2d');(function draw(){requestAnimationFrame(draw);const data=new Uint8Array(analyser.frequencyBinCount);analyser.getByteTimeDomainData(data);g.clearRect(0,0,canvas.width,canvas.height);g.strokeStyle='#00ff88';g.lineWidth=2;g.beginPath();const slice=canvas.width/data.length;let x=0;for(let i=0;i<data.length;i++){const v=(data[i]/128)-1;const y=(canvas.height/2)+v*(canvas.height/2-10);i?g.lineTo(x,y):g.moveTo(x,y);x+=slice}g.stroke()})()}}catch{};mediaModal.style.display='block';ensurePlayable(audio);return}if(type.includes('video')){el=document.createElement('video');el.controls=true;el.autoplay=false;el.style.maxWidth='100%';el.style.maxHeight='100%';el.playsInline=true;el.muted=false;const s=document.createElement('source');s.src=url+'#t=0.1';el.appendChild(s);mediaPlayer.prepend(el);activeMediaEl=el;mediaModal.style.display='block';ensurePlayable(el);return}if(type.includes('image')){el=document.createElement('img');el.alt=asset.title||'';el.src=url;el.style.maxWidth='100%';el.style.maxHeight='100%'}else if(type.includes('pdf')){el=document.createElement('embed');el.type='application/pdf';el.src=url;el.style.width='100%';el.style.height='100%'}else{const div=document.createElement('div');div.style.padding='16px';div.textContent='Preview not available';el=div}mediaPlayer.prepend(el);mediaModal.style.display='block'}
function closeMediaModal(){clearActiveMedia();mediaModal.style.display='none';tapOverlay.style.display='none'}
function playKey(keyNum){const asn=assignments.find(a=>Number(a.key_number)===Number(keyNum));if(!asn){show('error','No assignment for that key');return}const asset=asn.asset||{};playing={key:keyNum,asset};reflectPlaying();openMediaModal(asset)}
function stopPlayback(){playing=null;reflectPlaying();clearActiveMedia()}
;(function(){const modal=sheet,header=sheetHeader,grip=sheetResize;let dragging=false,startX=0,startY=0,startLeft=0,startTop=0;function onDown(x,y){dragging=true;const rect=modal.getBoundingClientRect();startLeft=rect.left;startTop=rect.top;startX=x;startY=y;modal.style.right='auto';modal.style.bottom='auto';document.body.style.userSelect='none'}function onMove(x,y){if(!dragging)return;const dx=x-startX,dy=y-startY;modal.style.left=(startLeft+dx)+'px';modal.style.top=(startTop+dy)+'px'}function onUp(){dragging=false;document.body.style.userSelect=''}header.addEventListener('mousedown',e=>onDown(e.clientX,e.clientY));document.addEventListener('mousemove',e=>onMove(e.clientX,e.clientY));document.addEventListener('mouseup',onUp);header.addEventListener('touchstart',e=>{const t=e.touches[0];onDown(t.clientX,t.clientY)},{passive:true});document.addEventListener('touchmove',e=>{const t=e.touches[0];onMove(t.clientX,t.clientY)},{passive:true});document.addEventListener('touchend',onUp);let resizing=false,rStartX=0,rStartY=0,startW=0,startH=0;function rDown(x,y){resizing=true;rStartX=x;rStartY=y;const rect=modal.getBoundingClientRect();startW=rect.width;startH=rect.height;document.body.style.userSelect='none'}function rMove(x,y){if(!resizing)return;const dx=x-rStartX,dy=y-rStartY;modal.style.width=Math.max(360,startW+dx)+'px';modal.style.height=Math.max(320,startH+dy)+'px'}function rUp(){resizing=false;document.body.style.userSelect=''}grip.addEventListener('mousedown',e=>{e.stopPropagation();rDown(e.clientX,e.clientY)});document.addEventListener('mousemove',e=>rMove(e.clientX,e.clientY));document.addEventListener('mouseup',rUp);grip.addEventListener('touchstart',e=>{e.stopPropagation();const t=e.touches[0];rDown(t.clientX,t.clientY)},{passive:true});document.addEventListener('touchmove',e=>{const t=e.touches[0];rMove(t.clientX,t.clientY)},{passive:true});document.addEventListener('touchend',rUp)})();
document.addEventListener('DOMContentLoaded',async()=>{populateDatalistsFromMemory();await loadAssignments();await unifiedSearch('');setInterval(loadAssignments,ASSIGNMENTS_REFRESH_MS)});
searchInput.addEventListener('input',()=>unifiedSearch(searchInput.value));
assignButton.addEventListener('click',assignSelectedToKey);
keyButtons.forEach(btn=>btn.addEventListener('click',()=>playKey(btn.dataset.key)));
stopButton.addEventListener('click',stopPlayback);
modalClose.addEventListener('click',closeMediaModal);
mediaModal.addEventListener('click',e=>{if(e.target===mediaModal)closeMediaModal()});
window.addEventListener('keydown',e=>{const k=e.key;if(/[1-5]/.test(k)){playKey(Number(k))}else if(k===' '){e.preventDefault();stopPlayback()}else if(k==='Escape'){closeMediaModal()}});
